# `features/sessions/components`

UI components owned by the sessions feature.

Use this package for components that are specifically about session management rather than generic shell tab chrome.

Session feature UI components.

Contains session-owned presentation for multi-session workflows. Shell-level composition belongs in `app/shell`, while session-specific UI belongs here.
