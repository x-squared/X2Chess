# `features/sessions/guards`

Session guard helpers.

These modules hold guard logic for dirty-session protection, close prompts, and related session lifecycle checks that should stay outside presentational components.

Session navigation and lifecycle guards.

Owns guard hooks that protect against unsafe navigation or session transitions.
