# `features/settings/hooks`

Hooks owned by the settings feature.

Add hooks here only when the state is settings-specific; generic persistence helpers should stay in `runtime/`.

Settings feature hooks.

Use this package for hooks that are specifically owned by settings workflows instead of the broader app.
