/**
 * ECO part — public API.
 *
 * Integration API:
 * - `buildEcoLookup`, `EcoEntry`, `EcoMatch` — see eco_lookup.ts.
 */

export { buildEcoLookup } from "./eco_lookup";
export type { EcoEntry, EcoMatch } from "./eco_lookup";
