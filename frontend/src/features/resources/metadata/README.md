# `features/resources/metadata`

Metadata schema editor UI for the resource feature.

Important files:

- metadata schema editor components
- `metadata_schema.css` for schema editor styling

This package is feature-owned UI. Persistence and schema validation live in `features/resources/services/schema_storage.ts`.

Resource metadata editing UI.

Contains metadata-field and schema-editor components that are specifically part of the resource workflow.
