# `features/training/components`

UI components owned by the training feature entry layer.

Use this package for components that coordinate training workflows at the feature boundary rather than reusable protocol/domain code.

Training feature UI components.

Contains feature-level training UI that integrates the training subsystem into the main application experience.
