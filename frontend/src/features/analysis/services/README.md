# `features/analysis/services`

Framework-free helpers for analysis feature logic.

Put reusable analysis computations or adapter-shaped helpers here when they are owned by the analysis feature but should not live inside a React hook or component.

Analysis feature services.

Use this package for non-UI analysis helpers that are owned by the analysis feature and do not fit better in `core` or shared engine packages.
