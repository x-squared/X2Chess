# `features/settings/services`

Framework-free helpers for the settings feature.

Use this package for settings-specific helpers that should not live inside React components, such as transformation or validation logic owned by the settings feature.

Settings feature services.

Contains settings-owned non-UI helpers that do not belong in generic runtime or storage packages.
