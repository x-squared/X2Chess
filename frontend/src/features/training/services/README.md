# `features/training/services`

Framework-free helpers used by training feature UI.

This package should stay small and focused on feature-layer helpers, not on the core training domain.

Training feature services.

Use this package for non-UI training helpers that are feature-scoped rather than part of the generic training domain.
