# `features/resources/web_import`

Feature-owned web-import UI helpers.

This package is the place for resource-feature UI pieces that are specifically about importing games from websites, while lower-level fetch/match/extract infrastructure lives in `resources/web_import`.

Resource web-import feature package.

Use this directory for web-import UI or feature logic that is owned by the resource feature rather than shared import infrastructure.
