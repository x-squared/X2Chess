# `features/settings/components`

Settings UI components.

Important files:

- `EditorStyleDialog.tsx`
- `DefaultLayoutDialog.tsx`
- `ExtDatabaseSettingsDialog.tsx`
- `SettingsPanel.tsx`

These components edit persisted runtime preferences and should stay focused on UI, preview, and validation behavior.

Settings UI components.

Contains settings dialogs and settings panels that present editable application preferences to the user.
